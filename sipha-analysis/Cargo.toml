[package]
name = "sipha-analysis"
version = "1.0.0"
edition = "2021"
description = "Grammar-agnostic scope extents and definition collection for sipha syntax trees (LSP support)"

[lints]
workspace = true

[dependencies]
sipha = { path = "../sipha", features = ["walk"] }
