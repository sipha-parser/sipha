# sipha-fmt

Formatter options and helpers for [sipha](https://docs.rs/sipha) syntax trees.

Re-exports sipha’s emit API and adds presets for common formatting (e.g. semantic-only output, or full round-trip with trivia).

See the main [sipha](https://github.com/sipha-parser/sipha) crate for the parser and tree APIs.
