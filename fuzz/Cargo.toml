[package]
name = "sipha-fuzz"
version = "0.0.0"
publish = false
edition = "2021"

[package.metadata]
cargo-fuzz = true

[dependencies]
libfuzzer-sys = "0.4"
once_cell = "1"
sipha = { path = "../sipha", default-features = true, features = ["incremental"] }

[workspace]
members = []

