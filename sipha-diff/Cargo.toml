[package]
name = "sipha-diff"
version = "1.0.0"
edition = "2021"
description = "Tree diff and comparison utilities for sipha syntax trees"

[lints]
workspace = true

[dependencies]
sipha = { path = "../sipha", features = ["emit"] }
