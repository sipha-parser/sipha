[package]
name = "sipha-sourcemap"
version = "1.0.0"
edition = "2021"
description = "Build span mappings from sipha transforms (map transformed spans back to original source)"

[lints]
workspace = true

[dependencies]
sipha = { path = "../sipha", features = ["transform"] }
